---
import { SERVICES as services } from "./services.ts";
import Card from "./Card.astro";
---

<div class="flex flex-col w-full items-center gap-8">
  {services.map(service => <Card desc={service.desc} dark={service.dark} />)}
</div>
